{
  "name": "library-database-system",
  "version": "1.0.0",
  "private": true,
  "description": "Library managment system for Imperial Sci-fi Fantasy Society",
  "scripts": {
    "dev": "concurrently -k \"pnpm --filter @library/api dev\" \"pnpm --filter @library/web dev\"",
    "start": "pnpm -r run start",
    "build": "pnpm -r run build"
  },
  "license": "UNLICENSED",
  "packageManager": "pnpm@12.3.4",
  "type": "module",
  "dependencies": {
    "@library/api": "workspace:*",
    "@trpc/server": "^11.0.0"
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "typescript": "~6.0.2"
  }
}
